{
  "name": "demarcelizer",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "bun install && bunx playwright install chromium",
    "dev": "bun --hot server.ts",
    "start": "bun server.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "latest",
    "@google/genai": "latest",
    "hono": "^4.7.0",
    "playwright": "^1.50.0"
  }
}
